<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %> - QUIZWIZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="/css/styles.css" rel="stylesheet" />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm sticky-top">
      <div class="container">
        <a class="navbar-brand fw-bold d-flex align-items-center" href="/">
          <img src="/images/Gemini_Generated_Image_im4qozim4qozim4q.png" alt="QUIZWIZ" class="site-logo me-2" />
          <span class="brand-text">QUIZWIZ</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain" aria-controls="navMain" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMain">
          <ul class="navbar-nav me-3 mb-2 mb-lg-0">
            <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
            <li class="nav-item"><a class="nav-link" href="/quizzes">Quizzes</a></li>
            <li class="nav-item"><a class="nav-link" href="/feedback">Feedback</a></li>
            <% if ((typeof session !== 'undefined') && session.user) { %>
              <li class="nav-item"><a class="nav-link" href="/quizzes/add">Add Quiz</a></li>
            <% } %>
          </ul>
          <form class="d-flex ms-auto" role="search" action="/quizzes" method="get">
            <input class="form-control me-2" type="search" placeholder="Search quizzes..." aria-label="Search" name="q" value="<%= (typeof query !== 'undefined' && query) ? query : '' %>">
            <button class="btn btn-primary" type="submit">Search</button>
          </form>
          <form class="ms-3" method="post" action="/logout">
            <% if ((typeof session !== 'undefined') && session.user) { %>
              <span class="me-2">Hi, <%= session.user.name || session.user.email %></span>
              <button class="btn btn-outline-secondary btn-sm" type="submit">Logout</button>
            <% } else { %>
              <a class="btn btn-outline-secondary btn-sm me-2" href="/login">Login</a>
              <a class="btn btn-primary btn-sm" href="/signup">Sign Up</a>
            <% } %>
          </form>
        </div>
      </div>
    </nav>
    <main class="container-fluid py-4">
      <%- body %>
    </main>
    <footer class="text-center py-4 text-muted">&copy; <%= new Date().getFullYear() %> QUIZWIZ</footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
  </body>
  </html>

