<% layout('layout') -%>
<h1 class="mb-3">My Scores — <%= quiz.title %></h1>
<div class="card">
  <div class="card-body">
    <table class="table table-dark table-striped align-middle mb-0">
      <thead>
        <tr><th>Score</th><th>Date</th></tr>
      </thead>
      <tbody>
        <% attempts.forEach(a => { %>
          <tr>
            <td><%= a.score %> / <%= a.total %></td>
            <td><%= new Date(a.created_at).toLocaleString() %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>





